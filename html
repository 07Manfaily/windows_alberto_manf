const EmptyPieChart = () => {
  return (
    <div style={{
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      height: '300px',
      backgroundColor: '#f8f9fa',
      borderRadius: '12px',
      padding: '40px',
      textAlign: 'center'
    }}>
      {/* Cercle vide simulant un pie chart */}
      <div style={{
        width: '120px',
        height: '120px',
        borderRadius: '50%',
        border: '8px dashed #d1d5db',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        marginBottom: '20px',
        backgroundColor: '#ffffff'
      }}>
        <span style={{ fontSize: '40px', color: '#9ca3af' }}>📊</span>
      </div>
      
      <h3 style={{
        margin: '0 0 10px 0',
        color: '#374151',
        fontSize: '18px',
        fontWeight: '600'
      }}>
        Pas de données disponibles
      </h3>
      
      <p style={{
        margin: 0,
        color: '#6b7280',
        fontSize: '14px',
        maxWidth: '280px',
        lineHeight: '1.5'
      }}>
        Les statistiques s'afficheront ici dès que des données seront disponibles
      </p>
    </div>
  );
};
